@model CartListViewModel
@{
}


<h2>Sepet</h2>

<table class="table table-responsive">
    <thead>
            <tr>
                <th>Id</th>
                <th>Ürün Adı</th>
                <th>Stok Adeti</th>
            </tr>
        </thead>
    @foreach (var cartLine in Model.Cart.CartLines)
    {
        
        <tbody>
            <tr>
                <td>@cartLine.Product.Id</td>
                <td>@cartLine.Product.Name</td>
                <td>@cartLine.quantity</td>
                <td><a class="btn btn-sm btn-danger" asp-action="RemoveFromCart" asp-controller="Cart" asp-route-productId="@cartLine.Product.Id">Sepetten Sil</a></td>
            </tr>
        </tbody>
    }
</table>